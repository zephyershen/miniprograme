<view class="error-mask" wx:if="{{show}}">
  <view class="error-card card">
    <view class="error-illustration" aria-hidden="true">
      <view class="error-circle">
        <text class="error-mark">!</text>
      </view>
    </view>
    <view class="title" wx:if="{{title}}">{{title}}</view>
    <view class="message">{{message}}</view>
    <view class="actions">
      <view class="btn btn-primary" bindtap="onConfirm">{{confirmText}}</view>
    </view>
  </view>
</view>
