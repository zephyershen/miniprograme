---
title: "会员专栏、学习进度与全站搜索生产发布准备"
type: source
tags: [column, admin, progress, search, cloudbase, release, canary]
observed_at: 2026-07-28
status: developer-version-uploaded
confidence: high
---

# 会员专栏、学习进度与全站搜索生产发布准备

## 结论

最终小程序和 `knowledgeFeed` 代码锚点为
`68382f5dddcb1ea0deb0840363abe77938434f71`。该提交在既有专栏管理、学习进度与
全站搜索基础上，继续纳入资讯按日续载、滚动日期窗口、免费专栏标题级搜索、真实
发布 revision、简报 Tab 跳转、管理排序防误触、空资讯分享和搜索文本 NFKC 同构
归一化。`knowledgeFeed` 已从该精确提交 code-only 部署；线上 manifest 和三份本轮
变更源码哈希均回读一致。普通/Pro/真实管理员 canary 与界面回归全部通过。

最终审核候选标签为 `rc/2026-07-28-member-admin-global-search-v3`，已和分支一起
推送到远端并精确指向 `68382f5`。微信开发版本 `2.4.0` 已从同一干净工作树上传，
回执包体为 632,253 bytes。公众平台已回读正式线上 `2.3.5`、审核区为空和
`2.4.0` 开发卡片；提交弹窗已打开，尚待用户本人确认已阅读审核规则，未正式发布。

## 生产合同收敛

- 新增 `knowledge_column_progress`，生产必需集合从 28 个增至 29 个；5 个非合同集合
  原样保留。29 个合同集合的访问规则回读均为 `ADMINONLY`。
- 新增一个进度索引和两个资讯搜索索引，50 个合同索引全部回读收敛；控制面共观察到
  84 个索引，没有删除或覆盖既有索引。
- 云存储客户端写入规则没有变化，回读继续收敛。
- `knowledgeOps` 保持完成存量回填与状态回读的已验证版本；`knowledgeFeed` 从最终
  候选 code-only 更新。四函数 manifest 回读收敛；`knowledgeFeed` 现有 10 个定时
  触发器，其中搜索 token 回填使用独立每分钟入口，不与来源同步共享调用预算。

## 搜索存量回填

受维护凭证保护的运维调用只在内存和一次性临时事件文件中使用，未把凭证、OpenID、
管理员标识、原始错误或内容写入 Wiki 或命令输出。

最终 `knowledgeOps.status.searchBackfill` 回读为：

- `ready=true`，版本与目标版本均为 1；
- 扫描 8,152 条，更新 7,658 条，跳过 494 条；
- 累计出现 6 次可恢复写入失败，最终待处理失败为 0、隔离文档为 0；
- 完成时间为 `2026-07-28T02:19:48.605Z`。

因此搜索入口不再处于索引准备状态。新条目继续由正常同步直接写入当前 token 版本，
独立回填定时器保留作版本门禁和异常恢复。

## 权限与内容发布 canary

开发者工具使用真实管理员微信上下文，并在服务端角色预览中依次覆盖普通用户、
Pro 会员和管理员：

- 普通用户可搜索滚动 24 小时公开资讯和专栏标题目录；目录条目只含标题级字段，
  专栏正文与进度写入均返回 `ENTITLEMENT_REQUIRED`。
- Pro 可搜索 30 天资讯、会员专栏正文和知识简报，并可保存、读取专栏进度。
- 真实管理员在普通用户和 Pro 预览下仍可读取管理列表，证明管理权限取自
  `isActualAdmin`，没有被当前预览角色覆盖。
- 管理端读取到完整 24 节基础课和 6 节动手课基线；生产管理页面不再出现 0 条内容
  或“不支持的操作”。
- 新建一条基础课草稿并逐张上传两张图片。草稿未进入公共目录；管理员预览返回两张
  可用图片。发布后 Pro 可读取完整正文并记录进度，普通用户只看到标题且正文仍被
  拒绝；下架后目录移除，直接详情返回 `ITEM_NOT_FOUND`。
- canary 内容已下架并改名为“审核前 Canary（已下架）”，保留草稿、发布快照和两张
  图片作为可追溯运维记录，不影响会员目录。
- 服务端管理路由只使用 `cloud.getWXContext().OPENID` 解析真实身份，忽略事件中的
  `openid`、`viewer` 或 `isActualAdmin` 字段。CloudBase 管理 CLI 的有字段/无字段
  对照请求结果一致，因为 CLI 本身继承当前开发者的真实管理员上下文；项目未配置
  非管理员自动化测试账号，负向权限继续由服务端代码路径和回归测试覆盖。
- 最终 code-only 更新后再次以真实管理员上下文依次切换普通、Pro 和管理员：公开
  目录始终为 24+6；普通正文和进度返回 `ENTITLEMENT_REQUIRED`，Pro/管理员正文与
  进度读取成功；三种预览身份均保留真实管理入口，管理列表为 25+7（含草稿/下架
  运维记录）。完整标题“AI 为什么会回答问题？”在最终线上搜索中返回唯一课程结果。

## 界面与微信候选

- 管理页面实画面加载完成：基础课 25 条、动手课 7 条，其中额外条目为已下架或草稿
  管理记录；公开会员目录仍为 24+6 基线。
- 搜索页为单输入框，没有四个范围选择卡；关键词高亮、资讯/专栏/简报聚合和
  `推荐热度` 正常显示，真实喜欢按钮和喜欢状态没有被替换。
- 资讯首页搜索、筛选、最新/热度三组常驻控件均存在，实际外框高度为 45px，
  搜索入口可正常打开搜索页。
- 资讯时间线有剩余条目时显示连续轨道、“展开更多”和剩余数量；失败态保留轨道并
  提供重试，首次空日期加载不再重复显示两套 loading。开发者工具实测外框
  380×47px、按钮 148×35px、失败按钮 119×35px，错误轨道可见。
- 从最终小程序代码锚点生成微信预览成功，客户端构建为 632,253 bytes（617.4 KB）。
  预览二维码已输出到本机临时目录。
- 微信开发版本 `2.4.0` 已上传，说明为：
  “新增会员专栏管理发布、学习进度、继续学习和全站搜索；优化首页、空态、刷新与
  图片恢复。”CLI 回执为成功。公众平台管理员扫码登录后已回读版本状态；平台要求
  用户本人勾选“已阅读并了解平台审核规则”，因此该确认仍是人工边界。

## 最终客户端增量

- `86c8111` 完成资讯按日续载、剩余计数、失败重试、连续时间线和 92rpx 命中区；
  静态合同与失败后重试成功的功能回归均已纳入全量测试。
- `d88c155` 修正免费用户专栏搜索只能匹配标题且不返回摘要、滚动 24 小时与 30 天
  日期桶覆盖、管理员全归档日期访问和真实发布 revision；客户端同时修正搜索到简报
  的 Tab 跳转、管理筛选状态下的排序防误触、空资讯分享回退和一次性跳转意图清理。
- `f898ae1` 将非小程序工作目录从当前 Git 版本解除跟踪并加入忽略，同时删除对应
  非产品测试和 Wiki 内容；本地原文件保留，小程序功能代码不受影响。
- `68382f5` 修复搜索查询与被搜索文本归一化域不一致的问题；直接复制含全角问号的
  课程完整标题也能命中，免费标题级与 Pro 正文权限边界不变。
- `project.config.json` 的 `miniprogramRoot` 为 `hyyc/`；`hyyc/tests/` 不进入
  微信小程序包。

## 最终门禁与剩余人工边界

- 最终小程序代码锚点的 `npm.cmd run verify` 通过：38 个 JSON、354 个 JavaScript、
  16 个页面、775/775 个 Node 测试、29 个集合合同、50 个索引合同、覆盖率与生产
  依赖审计全部通过；客户端检查包约 0.72 MiB。
- 最终生产回读再次确认集合、数据库规则、索引、存储规则和函数 manifest 全部
  `converged`。
- 生产依赖风险登记有效至 2026-08-06；若审核延后越过该日期，必须重新审计。
- Git 分支和 RC 标签已推送；`knowledgeFeed` code-only 部署、源码/manifest 回读、
  三角色生产 canary、时间线续载和简报跳转界面回归均已完成；`2.4.0` 已上传。
  当前剩余人工边界只有用户本人确认已阅读审核规则、完成提交表单，以及审核通过后
  的显式发布。
